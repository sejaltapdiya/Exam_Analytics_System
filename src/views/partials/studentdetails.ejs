<header>
    <div class="navbar navbar-dark bg-dark box-shadow">
      <div class="container d-flex justify-content-between">
        <a href="#" class="navbar-brand d-flex ">
          <img src="images/logo.jpg" alt="" width="30" height="24" class="d-inline-block">
          <strong>EXAM ANALYTICS SYSTEM</strong>
          <ul class="navbar-nav">          
            <li class="nav-item active">
              <% studentdetails.forEach(function(item) {%>
              <form class="form-inline" action="/studentprofile" method="POST">
                <button type="submit" class="btn btn-sm btn-info">
                 My Profile <input type="text" value="<%=item.name%>" name="name1"> </button> </form>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="#">Logout</a>
              </li>
          </ul>
          <% }) %>
         
          
        </a>
      </div>
    </div>
  </header>